[package]
name = "redirect-server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
maud = "0.25"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.5", features = ["trace"] }
